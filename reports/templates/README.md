# Report Templates

Use these templates when creating a new real experiment report under `reports/real_runs/<run_name>/`.

Keep TODO markers until the corresponding result, command, or qualitative example exists.
